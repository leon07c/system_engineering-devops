#!/usr/bin/env bash
# 8‑beheaded_process – forcibly kill any 7‑highlander process

# Get the PID(s) of the target process (full command‑line match)
pid=$(pgrep -f "7-highlander")

# If a PID was found, send SIGKILL to it
if [ -n "$pid" ]; then
  kill -9 $pid
fi
